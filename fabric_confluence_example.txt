# Fabric Example Page

This page demonstrates **all major formatting types** supported in Confluence when pasted from Markdown.

---

## 1️⃣ Headings

### H3 Example
#### H4 Example
Plain paragraph under an H4. You can nest headings like this to control structure.

---

## 2️⃣ Tables

| Feature | Description | Example |
|----------|--------------|----------|
| **Flows** | Data movement definitions inside Fabric. | `fabric flow run refdata-to-ui` |
| **Apps** | Runtime participants hosting services or clients. | `fabric app deploy pricing` |
| **Services** | Discoverable data endpoints (Flight, gRPC, AMPS). | `/discovery/services` |

---

## 3️⃣ Bullet & Numbered Lists

**Unordered list**
- Supports nested bullets
  - Second-level bullet
    - Third-level bullet
- Works with bold and *italic* text

**Ordered list**
1. Define schema  
2. Register flow  
3. Run app  
4. Query service  

---

## 4️⃣ Callouts / Panels

> **💡 Tip:** Use Flows for all data movement instead of direct JDBC jobs.  
> **⚠️ Warning:** Don’t modify secrets directly in the Platform Config; use the Secrets Manager.  
> **✅ Note:** Entitlements can be edited under the **Apps → Entitlements** and **Services → Entitlements** tabs.

---

## 5️⃣ Inline Styles

Normal sentence with **bold**, *italic*, and `monospace` words.  
You can also **combine _styles_** in one phrase.

---

## 6️⃣ Code Blocks

### Java
```java
public class FlowExample {
    public static void main(String[] args) {
        System.out.println("Hello, Fabric!");
    }
}
```

### JSON
```json
{
  "kind": "flow",
  "group": "redux1",
  "name": "refdata-to-ui",
  "source": { "type": "KDB" },
  "target": { "type": "FLIGHT" }
}
```

### Bash
```bash
fabric app start pricing-engine
fabric flow run refdata-to-ui
```

---

## 7️⃣ Inline Diagrams (ASCII)

```
          ┌───────────┐
          │  Source   │
          └─────┬─────┘
                │
          ┌─────▼─────┐
          │   Flow    │
          └─────┬─────┘
                │
          ┌─────▼─────┐
          │  Service  │
          └───────────┘
```

*Diagram: a simple data Flow connecting a Source to a Service.*

---

## 8️⃣ Mermaid Diagram (if macro available)

```mermaid
graph TD
  A[Source] --> B[Flow]
  B --> C[Service]
  C --> D[Client App]
```

*(If Confluence supports the Mermaid macro, this renders as a live diagram.)*

---

## 9️⃣ Links

- [Fabric Wiki](https://example.com/fabric/docs)
- [Discovery API](/discovery)
- [Local file reference](./fabric-configs/README.md)

---

## 10️⃣ Final Section

That’s it! 🎉  
You’ve seen examples of **headings, tables, lists, callouts, code blocks, inline styles, and diagrams** that all render correctly when pasted into Confluence.
